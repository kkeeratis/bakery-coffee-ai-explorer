import subprocess
import os
import sys

def run_bakery_app():
    # หาตำแหน่งโฟลเดอร์ปัจจุบันที่ไฟล์นี้อยู่
    current_dir = os.path.dirname(os.path.abspath(__file__))
    
    # ระบุชื่อไฟล์หลัก (ต้องตรงกับชื่อไฟล์ที่คุณเซฟโค้ดจาก Canvas)
    app_file = os.path.join(current_dir, "app.py")
    
    # ตรวจสอบว่ามีไฟล์ app.py อยู่จริงหรือไม่
    if not os.path.exists(app_file):
        print("="*60)
        print("❌ ไม่พบไฟล์ app.py!")
        print(f"ตำแหน่งที่ตรวจสอบ: {app_file}")
        print("-" * 60)
        print("วิธีแก้ไข:")
        print("1. นำโค้ดจาก Canvas ไปบันทึกเป็นไฟล์ชื่อ 'app.py'")
        print("2. นำไฟล์ 'app.py' มาวางไว้ในโฟลเดอร์เดียวกับไฟล์ 'launcher.py' นี้")
        print("="*60)
        return

    print("🚀 กำลังเริ่มระบบ Bakery AI Trend Explorer...")
    print("กรุณารอสักครู่... หน้าเว็บจะเปิดขึ้นใน Browser ของคุณอัตโนมัติ")
    
    try:
        # ใช้คำสั่งรัน streamlit ผ่านทาง python -m เพื่อลดปัญหาเรื่อง PATH
        # คำสั่งนี้เทียบเท่ากับ 'streamlit run app.py'
        subprocess.run([sys.executable, "-m", "streamlit", "run", app_file])
    except Exception as e:
        print(f"เกิดข้อผิดพลาดในการรัน: {e}")
        print("แนะนำ: ตรวจสอบว่าได้ติดตั้ง streamlit หรือยัง (pip install streamlit)")

if __name__ == "__main__":
    run_bakery_app()